# Important in-Built Functions 

## import java.util.Arrays;

1.Arrays.sort();
    
    Arrays.sort( arr ,start_index, end_index)
    Arrays.sort(arr,Collerctions.reverseOrder())
    
    [T, S] = [O(Nlog(N)), O(1)]
    
    1. for Integers
        by default sorts in ascending order
    2. for Strings
     Sorts alphabetically